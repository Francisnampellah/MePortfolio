// Stub for '@react-native/assets-registry/registry' — only used by react-native's
// local-image require() system, which this web docs site doesn't use (SVG icons only).
module.exports = {
  registerAsset: () => 0,
  getAssetByID: () => undefined,
};
